# brainfuck
A brainfuck compiler

Requires `gcc`

The first argument supplied to the compiler is the input brainfuck file, the second is the name of the output file. For instance:
```
brainfuck hello_world.bf hello_world.exe
```
